DOCKERHUB_USERNAME=shurikan117
DOCKERHUB_TOKEN=your-dockerhub-token
UMBREL_IMAGE=shurikan117/tao-umbrel:1.7.3
MIN_FREE_GIB=25

# --- docker compose runtime (Compose reads `.env` in the repo root — copy snippets there or export manually) ---
# Host directory for `./umbrel` content and `./umbrel/kopia` used by docker-compose.override.example.yml
# UMBREL_DATA=./umbrel
# Merge multiple Compose files without using docker-compose.override.yml:
# COMPOSE_FILE=docker-compose.yml:docker-compose.local.yml
